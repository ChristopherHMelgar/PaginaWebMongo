<!DOCTYPE html>
<html lang="en">
    <head>
        <% include ../partials/head %>
    </head>
    <body>
        <div class="container">
            <h3>Desea eliminar <%= producto.title %>?</h3>
            <%= typeof msg != 'undefined' ? msg : '' %>
            <form method="POST" action="/productos/<%= producto.id %>?_method=delete">
                <div class="row">
                    <div class="input-field col s8">
                        <input type="password" name="password" placeholder="Contraseña">                              
                    </div>
                </div>        
                <div class="row">
                    <div class="input-field col s8">
                        <button class="btn orange accent-3" type="submit">Borrar</button>
                    </div> 
                </div>                            
            </form>
        </div>
    </body>
</html>
